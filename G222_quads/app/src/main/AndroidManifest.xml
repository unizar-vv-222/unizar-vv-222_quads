<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".ui.quads.QuadModify"
            android:exported="false" />

        <activity
            android:name=".ui.reservas.ReservaModify"
            android:exported="false" />

        <!-- Lists -->
        <activity
            android:name=".ui.quads.G222_QuadsList"
            android:exported="false" />

        <activity
            android:name=".ui.reservas.G222_ReservasList"
            android:exported="false" />

        <activity
            android:name=".ui.quads.QuadDetail"
            android:exported="false" />

        <activity
            android:name=".ui.reservas.ReservaDetail"
            android:exported="false" />

        <activity
            android:name=".ui.reservas.ReservaSelectQuads"
            android:exported="false" />

        <activity
            android:name=".ui.reservas.ReservaConfirm"
            android:exported="false" />

        <activity
            android:name=".ui.quads.TestQuadActivity"
            android:exported="false" />

        <activity
            android:name=".ui.reservas.TestReservaActivity"
            android:exported="false" />

        <!-- Main activity -->
        <activity
            android:name=".ui.quads.G222_quads"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>